// . HTTP Server Events
export enum Event {
  Connect,
  Disconnect,
  RequestStart,
  RequestEnd
}

// . json get server
export class HttpServer {
  constructor(port = 80, host = '0.0.0.0') {
    //
  }

  connect() {
    //
  }

  registerHandler() {
    //
  }

  on() {
    //
  }
}
