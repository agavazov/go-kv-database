package app

// AppendQueueRequest when the service is warming up we can store the pending requests
// Store mutations and remove the duplicates (if the last line is duplicated)
func AppendQueueRequest() {
	// @todo
}

// RunWaitingQueue run all actions from the queue
func RunWaitingQueue() {
	// @todo
}
